[package]
license = "Apache-2.0"
name = "fuigo-version"
version = "1.0.12"
edition.workspace = true
description = "Lockstepped fuigo CLI version."

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
