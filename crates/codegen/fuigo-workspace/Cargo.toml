[package]
license = "Apache-2.0"
name = "fuigo-workspace"
version = "0.1.220-alpha.4"
edition.workspace = true
description = "Core host-local workspace library (FS, VCS, execution, discovery) for fuigo-shell and remote sampler"

[dependencies]
anyhow = { workspace = true }
arc-swap = { workspace = true }
dunce = { workspace = true }
fuigo-version = { workspace = true }
async-stream = { workspace = true }
async-trait = { workspace = true }
futures = { workspace = true }
fs2 = { workspace = true }
parking_lot = { workspace = true }
git2 = { version = "0.20", default-features = false, features = ["vendored-libgit2"] }
ignore = { workspace = true }
regex = { workspace = true }
rand = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = [
    "fs",
    "io-std",
    "macros",
    "process",
    "rt",
    "rt-multi-thread",
    "sync",
    "time",
] }
tracing = { workspace = true }
uuid = { workspace = true, features = ["v4", "v5", "v7"] }
fuigo-agent = { path = "../fuigo-agent" }
fuigo-tools = { path = "../fuigo-tools" }
fuigo-tools-api = { path = "../fuigo-tools-api" }
# `DiagHandle`, held by the hub config and threaded through `connect_hub`.
fuigo-diag-server = { workspace = true }
fuigo-workspace-client = { path = "../fuigo-workspace-client" }
# Daemon/process-lifecycle machinery for `bin/workspace_server.rs` only; the
# library never references it.
fuigo-workspace-daemon = { path = "../fuigo-workspace-daemon" }
fuigo-workspace-types = { path = "../fuigo-workspace-types" }
fuigo-config = { workspace = true }
fuigo-dirs = { workspace = true }
# Leaf config value types (RemoteSettings, BoolFlag) for the folder-trust decision.
fuigo-config-types = { workspace = true }
toml = { workspace = true, features = ["preserve_order"] }
dashmap = { workspace = true }
agent-client-protocol = { workspace = true }
fuigo-acp-lib = { workspace = true }
fuigo-codebase-graph = { path = "../fuigo-codebase-graph" }
fuigo-paths = { path = "../fuigo-paths" }
base64 = { workspace = true }
infer = { workspace = true }
sha2 = { workspace = true, features = ["force-soft"] }
bstr = "1.12.1"
hashbrown = "0.16.1"
nohash-hasher = "0.2.0"
rustc-hash = "2.1.1"
# Backs `file_system`'s fuzzy path picker (matcher + background daemon).
fuigo-fuzzy-file-search = { workspace = true }
indexmap = { workspace = true, features = ["serde"] }
smallvec = { workspace = true }
chrono = { workspace = true }
tree-sitter = "0.25.10"
tree-sitter-bash = "0.25"
url = { workspace = true }
fuigo-env = { workspace = true }
humantime-serde = { workspace = true }
prometheus = { workspace = true }
glob = "0.3"
fuigo-sandbox = { path = "../fuigo-sandbox", default-features = false }
fuigo-hooks = { path = "../fuigo-hooks" }
fuigo-hunk-tracker = { path = "../fuigo-hunk-tracker" }
# `metrics` enables the SDK's metric-donation client (periodic Prometheus
# registry gather → OTLP → hub donation pump); see metric_donation_reporter.
fuigo-computer-hub-sdk = { workspace = true, features = ["metrics"] }
fuigo-computer-hub-mcp-adapter = { path = "../../common/fuigo-computer-hub-mcp-adapter" }
fuigo-mcp = { path = "../fuigo-mcp" }
fuigo-file-utils = { path = "../fuigo-file-utils" }
fuigo-session-events = { workspace = true }
fuigo-auth = { path = "../fuigo-auth" }
fuigo-telemetry = { workspace = true }
fuigo-tty-utils = { workspace = true }
reqwest = { workspace = true }
fuigo-tool-protocol = { workspace = true }
fuigo-tool-runtime = { workspace = true }
fuigo-tool-types = { workspace = true }
tokio-util = { workspace = true, features = ["rt"] }
urlencoding = "2"
fuigo-fast-worktree = { path = "../fuigo-fast-worktree", features = ["metadata"] }
# tonic: Status/Code mapping for deploy errors in workspace_ops.
tonic = { workspace = true }
sha1 = { workspace = true, optional = true }
zip = { workspace = true, optional = true }

fuigo-fsnotify = { path = "../fuigo-fsnotify" }
clap = { workspace = true }
tracing-subscriber = { workspace = true }
# "enable" required for the SDK's spans to record at all.
fastrace = { workspace = true, features = ["enable"] }
fuigo-tracing = { workspace = true }
fuigo-extra-ca = { workspace = true }
tokio-tungstenite = { workspace = true, features = ["rustls-tls-webpki-roots"] }
tempfile = { workspace = true }
zstd = { workspace = true }
wait-timeout = { workspace = true }

# Raw fd/dirent/poll calls in foreign-session capability probes, the fuzzy
# walker's rlimit guard, and the envrc reader.
[target.'cfg(unix)'.dependencies]
libc = { workspace = true }

[target.'cfg(unix)'.dev-dependencies]
nix = { workspace = true, features = ["signal", "process"] }

[[bin]]
name = "fuigo-workspace-server"
path = "src/bin/workspace_server.rs"

[[bin]]
name = "workspace-server-probe"
path = "src/bin/workspace_server_probe.rs"

[features]
default = ["sandbox-enforce"]
compression = []
test-support = []
test-util = []
sandbox-enforce = ["fuigo-sandbox/enforce"]
default-bazel = [
    "sandbox-enforce",
    ]

[dev-dependencies]
tempfile = { workspace = true }
axum = { workspace = true }
tokio = { workspace = true, features = ["test-util"] }
fuigo-test-utils = { path = "../../common/fuigo-test-utils" }
# Decode tar.gz archives produced by workspace upload helpers in tests.
flate2 = { workspace = true }
tar = { workspace = true }

[package.metadata.cargo-shear]
ignored = ["tokio-tungstenite"]

[lints]
workspace = true
